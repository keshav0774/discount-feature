PORT=3000
MONGODB_URI=mongodb://127.0.0.1:27017/strike-discount
# Optional: without this Redis caching is skipped; MongoDB still works.
REDIS_URL=redis://127.0.0.1:6379
# The course/offer to use when no offerId is supplied to a discount endpoint.
DEFAULT_COURSE_ID=strike-hackathon-6
